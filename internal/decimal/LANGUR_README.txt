2024-06-28

This is an import of github.com/shopspring/decimal v1.4, with only minor modifications to make it work in langur as needed.

Presently, the only edit was in the decimal.go file, to the trigonometry function xatan() and its constants. We renamed the "go.mod" file as "go.mod.BAK".

Everything else is an added file, each starting with "langur", such as "langur.go", "langur_math.go", etc.

